%h1 Liste des consignes
//%table
//  %tr
//    %th Pièce
//    %th Température
//    %th Heure
//    %th Jour
//    %th{:colspan => "3"} Modification
//  - @setpoints.each do |setpoint|
//    %tr
//      %td= setpoint.room.name
//      %td= setpoint.temperature
//      %td= setpoint.times.strftime("%H:%M")
//      %td= setpoint.day_full
//      %td.colonneedit= link_to 'Voir', setpoint
//      %td.colonneedit= link_to 'Modifier', edit_setpoint_path(setpoint)
//      %td.colonneedit= link_to 'Supprimer', setpoint, method: :delete, data: { confirm: 'Are you sure?' }
%br

%table#setpoints_planning.table.table-bordered.table-condensed.span5
  %thead
    %tr
      %th Heure
      %th Dimanche
      %th Lundi
      %th Mardi
      %th Mercredi
      %th Jeudi
      %th Vendredi
      %th Samedi
  %tbody
    - for h in 0..23 # boucle sur les heures
      %tr
        %td
          = "0h#{h}"
        - for d in 0..6
          %td.sel_item{:rel => "popover", :title => "#{Mardi} - #{user.name}", :data => { "date" => day, "content" => assignments_popover_value(assignments, day), "assignments-url" => company_user_assignments_path(user.company, user, :day => day.to_s) }, :class => "#{'today' if Date.today == day}"},:'data-time' => "#{d}-#{h}"}
            = "#{d}-#{h}"

= render "mass_assignments"

= link_to 'Nouvelle consigne', new_setpoint_path